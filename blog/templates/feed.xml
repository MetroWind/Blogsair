<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ meta.Site.Name }}</title>
  <link href="{{ meta.Site.URIPrefix }}{{ url_for('feed') }}" rel="self" />
  <link href="{{ meta.Site.URIPrefix }}{{ url_for('index') }}" />
  <id>{{ meta.Site.FeedID }}</id>
  <updated>{{ meta.Site.Updated }}</updated>

  {% for post in pages -%}
  <entry>
    <title>{{ post.title }}</title>
    <link rel="alternate" type="text/html"
          href="{{ meta.Site.URIPrefix }}{{ url_for('page', path=post.path) }}" />
    <id>{{ post.FeedID }}</id>
    <updated>
      {%- if post.UpdatedISO -%}
        {{ post.UpdatedISO }}
      {%- else -%}
        {{ post.CreatedISO }}
      {%- endif -%}
    </updated>
    {%- if post.categories -%}
      {% for cat in post.categories %}
    <category term="{{ cat }}" />
      {%- endfor %}
    {%- endif %}
    <summary>{{ post.abstract }}</summary>
    <author>
      <name>{{ meta.Site.Author }}</name>
    </author>
    <content type="html">
    {{ post.html|e }}
    </content>
  </entry>
  {% endfor %}
</feed>
